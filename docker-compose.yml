services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vinyl
      POSTGRES_PASSWORD: vinyl
      POSTGRES_DB: vinyl_intelligence
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
